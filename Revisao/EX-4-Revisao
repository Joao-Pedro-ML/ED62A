#include <stdio.h>
#include <stdlib.h>


void ler(int *p){
    printf()
}

int main(){

    int *p, n=0;
    printf("Digite um número inteiro: ");
    scanf("%i", &n);
    p=(int *)malloc(n*sizeof(int));
    for(int i=0;i<n;i++){
        printf("Digite v[%i]: ", i);
        scanf("%i", &v[i]);
    }
    for(int i=0;i<n;i++){
        printf("v[%i] = %i\n");
    }
    free(p);
}
